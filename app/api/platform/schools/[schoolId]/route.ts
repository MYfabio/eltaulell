// Compatibility route. The local interface uses the static /update endpoint
// because it is more reliable in the Windows preview server.
export { PATCH } from "@/app/api/platform/schools/update/route";
